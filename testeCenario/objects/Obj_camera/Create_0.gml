/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

//alvo q vai seguir
target_ = Obj_kely;

//da camera
width_ = camera_get_view_width(view_camera[0]); //dentro da funcao coloca o viewport
height_ = camera_get_view_height(view_camera[0]);